<!DOCTYPE html>
<html lang="ko">
<head>
  <%- include('partials/head.ejs') %>
  <link rel="stylesheet" href="/css/searchresult.css">
</head>
<body>
  <%- include('partials/components/navBar') %>
  <h3><span class="searchType"><% if(searchType==='searchT'){%>제목<%}else{%>배우<%}%></span> <span class="searchWord"><%= searchWord%></span>에 대한 검색 결과 페이지 입니다. 총 <%= totalCount %> 개의 결과가 검색되었습니다.</h3>
  <div class="search-result-container">
    <div class="search-result-section">
    <% for(let i = 0 ; i <data.length ; i ++ ) {%>
      <div class="search-result-wrapper" id="result<%= data[i].dataValues.movieId %>">
        <div class="search-result-image">
          <img src="<%= data[i].dataValues.posterUrl%>" alt="">
        </div>
        <div class="search-result-content-box">
          <span class="search-result-content search-result-content-title">
            제목 : <%= data[i].dataValues.movieTitle  %>
          </span>
          <span class="search-result-content search-result-content-dir">
            감독 : <%= data[i].dataValues.directorNm  %>
          </span>
          <span class="search-result-content search-result-content-star">
            평점 : <%= data[i].dataValues.reviewMovieRating  %>
          </span>
          <span class="search-result-content search-result-content-cast">
            배우 : <%= data[i].dataValues.movieCast  %>
          </span>
        </div>
      </div>
    <% }%>
    <div class="show-next-page-btn"> </div>
    </div>
  </div>
  <footer class="footer"></footer>
</body>
<script>
</script>
<%- include('partials/tail.ejs') %>

<script src="/js/searchResult.js"></script>
</html>